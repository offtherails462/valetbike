<!-- Partial ERB for the site navbar -->

<nav class="navbar navbar-expand-lg navbar-dark bg-dark justify-content-between">
  <div class="container-fluid">
    <%= link_to("ValetBike", root_path, class: 'navbar-brand') %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <!-- Main links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <%= link_to("About", '#', class: 'nav-item nav-link') %>
        <%= link_to("Map View", '#', class: 'nav-item nav-link') %>
        <%= link_to("Stations", stations_path, class: 'nav-item nav-link') %>
        <%= link_to("Join", '#', class: 'nav-item nav-link') %>
      </ul>
      <!-- User management section -->
      <ul class="navbar-nav">
        <span class="navbar-text" href="#">
          Hello, <%= Current.user ? Current.user.first_name : 'guest' %>!
        </span>
        <% if Current.user %>
          <%= link_to("Change password", edit_password_path, class: 'nav-item nav-link') %>
          <%= button_to("Log out", logout_path, method: :delete, class: 'nav-item btn btn-primary') %>
        <% else %>
          <%= link_to("Log in", login_path, class: 'nav-item nav-link') %>
          <%= link_to("Sign up", signup_path, class: 'nav-item nav-link') %>
        <% end %>
      </ul>

      <!-- WIP dropdown implementation -->
      <!-- ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="btn btn-secondary dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Hello, <%= Current.user ? Current.user.first_name : 'guest' %>!
          </a>
          <li class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% if Current.user %>
              <%= link_to("Change password", edit_password_path, class: 'dropdown-item') %>
              <%= link_to("Log out", logout_path, class: 'dropdown-item') %>
            <% else %>
              <%= link_to("Log in", login_path, class: 'dropdown-item') %>
              <%= link_to("Sign up", signup_path, class: 'dropdown-item') %>
            <% end %>
          </li>
        </li>
      </ul-->
    </div>
  </div>
</nav>
